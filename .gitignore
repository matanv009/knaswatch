# KnasWatch stores no secrets in the repo by design (see SECURITY.md).
# These rules are belt-and-suspenders in case a user redirects output here.

*.log
*.debug.json
knaswatch-debug/
browser-profile/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Local working notes, not part of the published project.
HANDOFF.md

# Personal helper scripts: they contain real profile nicknames, so they stay local.
setup_profiles.py
add_recipient_local.py

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
